[project]
name = "claudinho"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.9",
    "feedparser>=6.0.12",
    "openai>=2.33.0",
    "pydantic>=2.0",
    "pyyaml>=6.0.3",
    "Pillow>=11.0",
]

[project.scripts]
claudinho = "main:main"
