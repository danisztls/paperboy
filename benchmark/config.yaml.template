urls:
  - https://www.youtube.com/watch?v=XXXXXXXXXXX   # YouTube video (transcript)
  - https://example.com/some-article              # article (fetched + summarized)

models:
  - provider: deepseek           # one of: deepseek, gemini, claude_cli
    model: deepseek-v4-flash
  - provider: gemini
    model: gemini-2.5-flash
  # - provider: claude_cli       # reuses the local `claude` CLI login
  #   model: sonnet
